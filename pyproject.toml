[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Digital Literacy Curriculum"
authors = [{name = "Daniel Corks", email = "d.corks@gmail"}]
dynamic = ["version", "description"]
